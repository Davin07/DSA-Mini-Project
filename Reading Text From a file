#include<stdio.h>
#define SIZE 50
void main()
{

    FILE *fc;
    fc = fopen("data.txt", "r");
    int n;
    int e = 50;
    char st[SIZE];
    int s[50];
    fscanf(fc,"%d", &n);
    
    /*for (int i = 0; s[i -1] != 0; i++)
    {
        fscanf(fc, "%d", &s[i]);
    }*/
    
    printf("%d \n",n);


    /*for (int i = 0; s[i - 1] != 0; i++)
        printf("%d ", s[i]);
    }*/
    
    while(fgets(st,20, fc) != NULL)
    {puts(st);}
        
    //fscanf(fc,"%s", st);
    
    

    fclose(fc);
}
